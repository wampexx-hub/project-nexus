# Next Steps

1. **Start Database**: The API needs a running PostgreSQL instance.
   - Install Docker Desktop and run `docker-compose up -d`.
   - OR update `apps/api/.env` with a real `DATABASE_URL`.
2. **Frontend Auth**: I will now build the Login/Register pages in `apps/web`.
